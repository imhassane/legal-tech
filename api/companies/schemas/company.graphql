type Company @key(fields: "id") {
    id: ID!
    name: String!
    prefecture: String!
    cover: String!
    createdAt: DateTime!
    description: String!
    identification: String!
    members(start: Int!, limit: Int!): CompanyMembers!
}