type Domain @key(fields: "id") {
    id: ID!
    name: String!
    description: String!
}


extend type Company @key(fields: "id") {
    id: ID! @external
    domains: [Domain!]!
}

extend type Member @key(fields: "id") {
    id: ID! @external
    domain: Domain!
    domains: [Domain!]!
}