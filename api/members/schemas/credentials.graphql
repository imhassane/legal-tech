enum Status {
    PENDING
    APPROVED
    VALIDATED
    BLOCKED_TEMPORARILY
    BLOCKED_FOREVER
    DELETED
}

type Credentials {
    id: ID!
    email: String!
    status: Status!
    insertedAt: DateTime!
    updatedAt: DateTime!
}